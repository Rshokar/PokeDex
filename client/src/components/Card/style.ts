import React from "react";

const container: React.CSSProperties = {
    backgroundColor: "#ffffff",
    width: "90vw",
    height: "60vh",
    borderRadius: "40px",

}

export default {
    container,
}