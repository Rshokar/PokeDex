import React from 'react'

const wrapper: React.CSSProperties = {
    position: 'fixed',
    top: "0%",
    height: '40%',
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'center',
    alignItems: 'center',
    width: '100%',
}

const image: React.CSSProperties = {
    width: '50%',

}

const style = { wrapper, image }

export default style

