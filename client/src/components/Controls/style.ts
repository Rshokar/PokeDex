import React from 'react'

const wrapper: React.CSSProperties = {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'start',
    alignItems: 'center',
    width: '90%',
}

const style = { wrapper }

export default style