import React from "react";

const container: React.CSSProperties = {
    backgroundColor: "#ffffff",
    width: "90vw",
    padding: "10px",
    paddingTop: "20px",
    paddingBottom: "20px",
    borderRadius: "40px",
}

export default {
    container,
}