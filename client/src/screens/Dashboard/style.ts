import React from "react";


const container: React.CSSProperties = {}

const floatingButton: React.CSSProperties = {
    position: 'fixed',
    bottom: '80px',
    right: '20px',
}
export default {
    container,
    floatingButton
}