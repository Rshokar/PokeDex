import React from 'react'
import style from './style'

const { container } = style;

type Props = {}

const Home = (props: Props) => {
    return (
        <div style={container}>Home</div>
    )
}

export default Home